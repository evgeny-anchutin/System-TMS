
Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	Штрихкод = РегистрыСведений.ШтрихкодыДокументов.ШтрихкодДокумента(Ссылка);
	Если Не ЗначениеЗаполнено(Штрихкод) Тогда
		Штрихкод = РегистрыСведений.ШтрихкодыДокументов.ГенерироватьШтрихкод(Ссылка, Истина);
	КонецЕсли;
	
КонецПроцедуры
